**Hypothesis #1: Permit Bureaucracy Bottleneck (Processing Delays)**
The Problem: Unreasonable delays between submitting an application and getting approval hold up housing construction. Measuring exactly where these delays happen can pinpoint municipal inefficiency.
**APPLICATION DATE to ISSUE DATE:** Calculate the duration (in days) to see how long it takes for a permit to get approved.
**ISSUED BY & WORK TYPE:** Group the data to see which municipal departments or specific permit types (e.g., Residential vs. Commercial) experience the longest wait times.
**APPLICATION DATE to FINAL DATE:** Measure the absolute lifespan of a construction project from its first submission to final sign-off.

**Tree Regression model, Chronological Train-Validation-Test Split (70% / 15% / 15%).**
Predictive Machine Learning Extension (Scikit-Learn Pipeline)
*   **The Predictive Goal**: Move from historical reporting to forward-looking resource allocation by predicting total permit processing time (Duration in days) at the exact moment a developer submits an application.
*   **Feature Leakage Prevention Constraints**: 
To maintain strict real-world validity, the feature matrix X is entirely restricted to information known at the municipal intake desk at the exact moment of submission, such as WORK_TYPE, WARD, initial CONSTRUCTION_VALUE, and APPLICATION_MONTH. Any metrics recorded later in the permit lifecycle—such as FINAL_DATE or late-stage inspection remarks—are strictly excluded. This strict boundary prevents data leakage and ensures the model simulates a true production environment capable of forecasting processing delays for newly incoming applications.


Phase 0
~~I imported my CSV file into PostgreSQL database through the pgAdmin app/interface for PostgreSQL. From database, you drop down to schemas, then public, then right click the Table option to create your own table as PostgreSQL requires you to build the empty table structure before you can import a CSV. Define your columns to match your CSV headers~~ 
~~This is actually a tedious way so i’m going to do it with Python pandas & sqlalchemy to automatically create the table and load the data into PostgreSQL!~~CROSSED OUT FOR ELT (**EXTRACTION, LOAD, TRANSFORM**) revamp for a complete, live updated data pipeline!
After 3 weeks of inactivity (due to finishing exams), the data's rows increased from 75501 to 75911, demonstrating the live data pipeline effect.
**_EXTRACTION_**:_ #1 EXTRACTING THE DATA_ first, i connected to PostgreSQL database.
Sqlalchemy: the bridge to connect to PostgreSQL.   Address: postgresql+psycopg2://username:your_password@localhost:port/Building_Permits_KW 
Postgresql is the language we’re connecting to.
Psycopg2 library: Python needs a translator to talk to Postgresql
After buidling a postgresql engine to connect & upload the data, I neede to bypass ArcGIS Server cursor enforcement that limits 2000 rows per data request load for my database which has 75000+ rows, I leveraged pagination, splitting dataset into smaller chunks to extract data from API, with a while loop. Using the requests library, I was able to call "get" requests to interface with the ARCGIS API endpoint, and I formatted parameter dictionaries with "f: 'json'" to transform query responses as JSON structures instead of HTML.
_#1GETTING THE SPATIAL COORDS_
In order to get spatial coords, I wrote an if/else check to grab the `x` and `y` points and save them as flat `X` (Longitude) and `Y` (Latitude) columns in my final list, since the coords were hidden inside a separate `geometry` object.  ~~I applied it as a parameter to my .to_sql function & it uploaded the kw_building_permits data inside my PostgreSQL database in Dbeaver. I initially opened the data table in pgadmin$ first, but then I realized DBeaver is a more profesionally-used tool instead of pgadmin so I switched to opening it in DBeaver using the same tree path in pgadmin: databases, file, schemas, public, then table.~~
_#3 CLEANING MESSY DATES: _ The API stores dates as messy millisecond timestamps inside strings. I used `pd.to_numeric()` with `errors='coerce'` to safely turn those strings into numbers. I used `pd.to_datetime(unit='ms')` to convert those large milliseconds into standard pandas datetime tracking objects, and `.dt.date` arguement to STRIP the TIMESTAMP to keep year-month-day format with no time.
**_LOAD_**: I used `datafile.to_sql()` and  connected to a SQLAlchemy engine to push the data into my local `Building_Permits_KW` database.
I created a brand-new table named kw_building_permits inside my PostgreSQL database and filled it with data.
Now can I begin to do some data cleaning/transformation.
_**TRANSFOM:**_ 
**Data Cleaning with SQL in DBeaver:** permits.sql
1. Duplicates: for this dataset, there's no need to do find fuplicates & remove as it's highly unlikely to contain strict system-level duplicates.
2. Handling NULL values: Work Type & Final Date columns are lots of null values or empty strings. Final Date column makes sense as there exist building construction that are yet to be finish. Work Type has a lot of empty value because municipalities leave this blank for simple projects (i.e. minor renovations) so it often requires manual data entry by the applicant, which leads to missing data. APPLICATOIN DATE & ISSUE DATE & ISSUED BY: these 3 columns have 6 rows affected in total and since these var are important, we'll just filter out those rows. I used COALESE to label nulls in Work Type as 'Unknown' and nulls in Final Date as 'Unfinished'
Construction_Value: Because construction values are highly skewed (lots of small residential projects, a few multi-million dollar developments), I used the median value to fill in blanks. I found the median value through using 2 CTE.

**Bringing back to Jupyter Notebook:**
To bring my cleaned data in DBeaver back into my VsCode JupyterNotebook, I made sure that my permits.sql's file path would point to the same location in vscode in order for the sql file to also appear in VSCODE. Once I refreshed & the permits.sql appeared in VSCODE, I read the file the with open & read method & saved the file to a variable, and finally saved it as a clean_file var using pd.read_sql() with the engine as a parameter to connect & pull my previously loaded data back out of PostgreSQL. 
*  _Data engineering a WARD col:_
It is crucial for my project's hypothesis to have a ward column, hence I put in the effort to build a ward column by mapping coordinates.
To create a WARD column, I first used the Point function from the shapely.geometry library to loop through my X and Y columns, tying them together to create Point(xy) spatial geometry objects. I converted my standard pandas DataFrame into a geopandas.GeoDataFrame and attached my new geometry list and assigned it a Coordinate Reference System (`crs="EPSG:4326"`). This officially transformed my raw coordinates into geospatial data that is ready for mapping to the Litchener wards data that shall correspond to these coordinates.  I used Kitchener's Ward Boundary data from a live GEOJSON link (this is because I realized I could not download the GEOJSON file as it was taking forever so I resorted to using a GEOJSON link). I uploaded the Kitchener Wards data into a geodataframe & converted it to the same CRS as our permits data for accurate spatial analysis. Then, I perform a WITHIN spatial join to associate each permit with the ward it falls WITHIN. I now have a WARD (str col) and a WARDID (int col) in which both's data are numbers.
*  _Pinpointing the municipal inefficiency:_
APPLICATION DATE to ISSUE DATE: Calculate the duration (in days) to see how long it takes for a permit to get approved.
Grouped by PERMIT_TYPE: Institutional & Gov has the longest approval w/ avg 49 days, followed closely by Commercial & Industrial w/ avg 43 days. Then, Residential has avg 28 days, Others/Mixed Approval avg 26 days, Trades & Utilities avg 11 days.
Grouped by WORK_TYPE: Building Conversion has the longest approval (avg 45 days), followed by Demolition (44 days), and Addition (37 days).
Filtered Outliers: 200-400 approval duration days are delayed extended projects affected by roadblocks, and 400-3800++ days can be stalled/abandoned projects or caused by faulty data entry.
PROJECT_DURAITON: ISSUE DATE to FINAL DATE: Measure the supply chain health at its time period, contractor speed, construction efficiency 
Grouped by PERMIT_TYPE: Residential has the longest project duration w/ avg 1.3 years, followed by Institutional & Gov w/ 1.1 years, then Commercial & Industrial (1 year). 
Grouped by WORK_TYPE: New Construction has the longest duration w/ avg 1.7 nearly 2 years, followed by Septic System w/ avg 1.4 years, and Addition w/ avg 1.2 year. 

*  _Tree model prediction:_
model specifically predicts the approval_duration for kitchen-related building permits using features such as ward, application_month, permit_type, work_type, and construction_value. From a business perspective, accurate timeline forecasting enables contractors, business owners, and residents to efficiently schedule trades and budget projects. By isolating high-volume, standard applications (filtering projects under a $400,000 construction value threshold and capping timeline outliers at 60 days), the model isolates routine municipal workflows from complex, multi-round commercial developments that follow vastly different zoning and evaluation rules. The final validation phase yielded a major breakthrough, establishing HistGradientBoosting as the top-performing model. The optimized gradient boosting model successfully achieved a Validation Adjusted R2 of 0.3240. Crucially for city stakeholders, it delivered a Mean Absolute Error (MAE) of just 9.02 days and a Root Mean Squared Error (RMSE) of 11.73 days. Explaining nearly 32.4% of the operational variance while maintaining an average error margin under two business weeks makes this model highly viable for integration into internal city planning dashboards or public-facing tracking portals.
